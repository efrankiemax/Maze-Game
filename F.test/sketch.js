function setup(){
	createCanvas(windowWidth,windowHeight);
	background(255);
}
function draw(){
background(255);
	 // let c = [60,-60];
	 // c = random(c);
	 for(let y =round(random(windowHeight/2 -75)); y<=windowHeight/2 -75; y = y+round(random(60,200))){
				print(y);
				for(let x =round(random(windowWidth)); x<=windowWidth; x = x+round(random(60,200))){

				}
	}
}
